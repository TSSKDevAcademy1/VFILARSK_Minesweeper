package register;

import java.io.BufferedReader;
import java.io.FileInputStream;
import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;
import java.io.ObjectInputStream;

public class SuperRegisterLoader implements RegisterLoader {

	@Override
	public void save(Register register) throws IOException {
		FileWriter fWriter = new FileWriter("dvojzoznam.txt");
		for(int i =0; i< register.getCount();i++){
			fWriter.write(register.getPerson(0).getClass() + "\n" + register.getPerson(0).getPhoneNumber());
		}
		fWriter.close();
	}

	@Override
	public Register load() throws ClassNotFoundException, IOException {
		FileReader fReader = new FileReader("dvojzoznam.txt");
		BufferedReader buffReader = new BufferedReader(new FileReader("dvojzoznam.txt"));
		String name;
		Number number;
		int i = 1;
		if(mod(1,2) == 0)
		while(buffReader.readLine() != null){
			bu
		}
		return null;
	}

}
